[package]
name = "mpi-transaction"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Transactional messaging support for mpi-rs"

[dependencies]
mpi = { path = "../mpi" }
persistent-log-storage = { path = "../persistent-log-storage" }
