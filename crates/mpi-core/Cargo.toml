[package]
name = "mpi-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = []
enable-mpi-transaction = []

[dependencies]
ctx-future = { path = "../ctx-future" }
mpi-macros = { path = "../mpi-macros" }
