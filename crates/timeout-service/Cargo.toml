[package]
name = "timeout-service"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
mpi = { package = "mpi-core", path = "../mpi-core" }
